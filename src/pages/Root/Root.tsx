import Link from '../../components/Link';

const Root = () => {
    return (
        <>
            <h1>root</h1>
            <Link path="/about">about</Link>
        </>
    );
};

export default Root;
